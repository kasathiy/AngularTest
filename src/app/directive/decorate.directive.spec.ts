import { DecorateDirective } from './decorate.directive';

describe('DecorateDirective', () => {
  it('should create an instance', () => {
    const directive = new DecorateDirective();
    expect(directive).toBeTruthy();
  });
});
